# Copy to .env only for local tooling that loads dotenv files.
# OryxOS reads secrets from process environment variables.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
NVD_API_KEY=
SERVER_PORT=8080
