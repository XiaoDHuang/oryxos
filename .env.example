# Copy to .env only for local tooling that loads dotenv files.
# OryxOS reads secrets from process environment variables.
# Model choice lives in each Profile (.oryxos/profiles/*.yaml), not here.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
KIMI_API_KEY=
KIMI_BASE_URL=https://api.moonshot.cn
NVD_API_KEY=
SERVER_PORT=8080
